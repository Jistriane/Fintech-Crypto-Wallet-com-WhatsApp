{
  "name": "crypto-wallet",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd crypto-wallet-admin && npm run dev",
    "build": "cd crypto-wallet-admin && npm run build",
    "start": "cd crypto-wallet-admin && npm run start",
    "lint": "cd crypto-wallet-admin && npm run lint"
  }
}